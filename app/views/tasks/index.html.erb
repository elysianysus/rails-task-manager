<h1>My Tasks</h1>

<table class="table">
  <thead>
  <tr>
      <th scope="col">Done?</th>
      <th scope="col">Tasks</th>
  </tr>
  <% @tasks.each do |task| %>
  <tr>
   <td>
      <input type="checkbox" <%= "checked" if task.completed? %> />
    </td>
    <td>
      <%= link_to task.title, task_path(task) %> </td>
  </tr>
  <% end %>
  </tbody>
</table>

<br>
  <%= link_to "Add", tasks_new_path %>
